import XCTest
@testable import HelloKit2Package

final class HelloKit2PackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(HelloKit2Package().text, "Hello, World!")
    }
}
